<IfModule mod_rewrite.c>
    RewriteEngine On

    # Send everything to the public folder without changing the URL
    RewriteCond %{REQUEST_URI} !^/public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
